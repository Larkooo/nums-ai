[project]
name = "nums-ai"
version = "0.1.0"
description = "Neural network that learns to play NUMS — a fully on-chain number-placement game on Starknet"
requires-python = ">=3.10"
dependencies = [
    "mlx>=0.22.0",
    "numpy>=1.24.0",
    "gymnasium>=0.29.0",
]
